import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  input: {
    backgroundColor: '#fff',
    color: '#000',
  },
  errorText: {
    color: '#B41230',
    fontSize: 14,
    marginTop: 2,
    marginLeft: 2,
  },
});

export default styles;
